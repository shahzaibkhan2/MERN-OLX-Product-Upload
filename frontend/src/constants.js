export const apiUri = {
  baseUri: String(import.meta.env.VITE_BASE_URI),
  productsUri: String(import.meta.env.VITE_PRODUCT_URI),
  categoriesUri: String(import.meta.env.VITE_CATEGORY_URI),
  usersUri: String(import.meta.env.VITE_USERS_URI),
};
